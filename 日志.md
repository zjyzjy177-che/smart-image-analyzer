# 项目工作日志

> 智能图片分析系统 · z24281213 小组

---

## 组员 A（岳思铭）

### 2026-07-22

-

### 2026-07-23

- 完成**图像分类模块**：使用 torchvision 官方 ResNet50 权重、预处理流程及 ImageNet 标签，移除第三方标签下载依赖
- 完善分类输入兼容：支持 RGB、RGBA、灰度图及浮点图片
- 完成**OCR 文字识别模块**：兼容 PaddleOCR 2.x/3.x，并加入 EasyOCR 自动备选方案
- 完善 OCR 输入兼容与文字区域标注，支持中英文识别结果换行展示
- 将图像分类与 OCR 正式接入 `app.py`，补充错误信息展示
- 完成 Windows/Python 3.13 兼容适配：加入 `audioop-lts`，OCR 自动选用已验证的 EasyOCR 后端
- 完成分类、OCR 单模块自测及 `app.py` 完整调用链测试
- 修复 Gradio 首页 500：限制 Pydantic、FastAPI、Starlette 为 Gradio 4.44 兼容版本，并完成 HTTP 200 首页测试
- 重构图像分类：增加 Top-5、中英文标签、中文大类、可信度等级与低可信提示
- 增加人物内容判断，解决 ImageNet 无通用“人物”类别导致人像样例误判的问题
- 完成 OCR 中英文混排测试：正确识别“北京交通大学”和“Smart Image 2026”
- 接通分类与 OCR 置信度阈值：分类低于阈值判为不确定，OCR 过滤低可信文字并显示逐段置信度
- 完成高低阈值对照测试，确认网页滑块会实际改变分类采纳状态与 OCR 过滤结果
- 为置信度滑块绑定自动刷新事件，拖动结束后无需再次点击“开始分析”
- 将图像分类升级为整图语义理解：集成 BLIP 图像描述，输出场景大类、中文摘要及英文原始描述
- 完成 BLIP 本地缓存与离线优先加载，避免启动时反复联网；项目样例识别为红牛赛车服人物
- 重构多语言 OCR：增加语言下拉选择，按文字体系按需加载并缓存 EasyOCR Reader
- 支持简繁中文、日语、韩语、欧洲语言、俄语、阿拉伯语、印地语、泰语和越南语
- 将置信度阈值下限调整为 0，增强浅色、小字和低对比文字检测
- 完成俄语“Привет мир 2026”、中文浅色小字及高低阈值对照测试
- 配合项目升级至 Gradio 6.20 与 Transformers 5.x，解决新版 Hugging Face 依赖冲突
- 界面非结果区域改为中文微软雅黑、英文 Times New Roman 混排字体
- 恢复整图描述、场景大类、人像补充判断及 Top-5 参考分类的完整展示
- 集成 MarianMT 离线英译中模型，以 BLIP 原始描述为源生成中文描述
- 根据 BLIP 逐词概率计算描述生成可信度，并由滑块控制“可信/仅供参考”状态
- 增加 OCR 自动语言模式：上次成功语种优先、可靠结果提前停止，避免遍历模型卡顿
- 修复 Windows 启动脚本：优先使用 `py -3`，并校验 Gradio 6 与 Transformers 5

### 2026-07-24

-

---

## 组员 B（李汇洋）

### 2026-07-22

- 克隆项目仓库，搭建本地开发环境
- 修复环境兼容问题：降级 `huggingface-hub` 至 0.36.2、修补 `gradio_client` bool 类型错误
- 完成**任务 1「人脸检测」**：集成 MTCNN 为主检测器，DNN 多尺度为备选，Haar Cascade 兜底
- 下载 DNN 模型文件（deploy.prototxt + caffemodel，共 10.6MB）
- 将人脸检测模块接入 `app.py` 网页端

### 2026-07-23

- 优化人脸检测：调整 MTCNN 置信度阈值（0.9 → 0.7），提升侧脸检出率
- 完成**任务 2「启动脚本」**：修复 `run.bat` / `run.sh` 的编码兼容、补全依赖检查、启动后自动打开浏览器
- 完成**任务 3「风格迁移」**：实现素描黑白线条画和二次元动漫风格，均为纯 OpenCV 算法，秒级出结果
- 更新 `requirements.txt`：加入 `facenet-pytorch`，固定 `huggingface-hub` 版本
- 将风格迁移模块接入 `app.py`，添加风格选择下拉框
- 将置信度阈值滑条扩展为物体检测和人脸检测通用
- 更新 `README.md`：补全任务 1/3 的详细实现原理和三阶段级联说明
- 提交并推送至 GitHub（commit `a4f8e9c`、`467c00c`）
- 修复组长拉取代码后 `app.py` 启动报错：`css`/`head` 参数需在 `gr.Blocks()` 而非 `launch()` 中使用（Gradio 4.x 兼容问题）

### 2026-07-24

- 待定

---

## 组员 C（张建宇）

### 2026-07-22

- 创建 GitHub 仓库 `smart-image-analyzer`，初始化项目目录结构和 `.gitignore`
- 搭建项目骨架：编写 `detector.py`（YOLOv8 物体检测模块）
- 编写 `app.py` 主程序，基于 Gradio 搭建 Web 界面框架
- 为组员 A/B 编写初始模块框架：`classifier.py`、`ocr.py`、`face_detect.py`、`style_transfer.py`
- 编写 `README.md`：项目说明、分工表、各模块技术要点、开发步骤指南
- 首次提交并推送至 GitHub（commit `c2f42f4`）

### 2026-07-23

- **界面大改版**：将 Gradio 默认主题替换为 BJTU 蓝白风格
- 集成学院背景图（透明度 0.20）+ 校徽至页面顶部
- 重新设计顶部导航栏：校徽 + 北京交通大学 | 智能图片分析系统 双标题布局
- 实现左右分栏布局：左侧功能侧栏 + 右侧主工作区
- 优化侧栏交互：功能按钮可点击切换，联动 Gradio Radio 组件
- 修复 Gradio 6.x 兼容性问题：将 `theme`/`css`/`head` 参数移至 `launch()` 方法
- 重写 CSS：交大标准蓝 `#0055B3` 配色体系，圆角卡片、渐变标题栏、按钮悬停效果
- 解决侧栏 Radio 隐藏不占空间问题：使用 `position:absolute` 移出屏幕
- 修复结果文字跨平台颜色问题：将 `gr.Textbox` 替换为 `gr.HTML` + 内联样式（朱红 `#CC3300`）
- 调整 UI 细节：增大字体（1.15em~1.2em）、加宽结果列比例（3:2→2:3）、结果居中显示
- 删除造成空白矩形的 `param-box` 边框包装
- 添加 emoji 至功能按钮和卡片标题
- 修复 `run.sh` 启动脚本：添加 `cd "$(dirname "$0")"` 确保路径正确、后台启动自动打开浏览器
- 配置 git 追踪校徽和背景图（`!images/badge.png`、`!images/bg.jpg`）
- 跨平台字体兼容：添加 `PingFang SC`（macOS）、`Noto Sans SC`（Linux）兜底字体
- 多轮调试：响应组长反馈进行 10+ 次 UI 迭代优化
- 提交并推送至 GitHub（commit `e4d9023`、`6031b1f`、`7bd506f`、`a956398`）

### 2026-07-24

- 修复跨平台楷体渲染不一致问题，最终确定结果文字字体为黑体（`Heiti SC`/`SimHei`）
- 集成组员A的图像分类和OCR模块至 `app.py`
- 升级 YOLO 检测模型：`yolov8n.pt` → `yolov8m.pt`（Medium），提升物体检测精度
- 修复物体检测结果 `<br>` 被 HTML 转义显示为原文的问题
- 精简图像分类输出，移除与物体检测重复的 Top-K 候选列表
- 扩展 OCR 多语言支持：中日韩 + 欧洲多语 + 阿拉伯/印地/泰/越南（8 个 EasyOCR Reader 分组）
- 修复 Gradio 6.x 兼容性问题：Radio 用 `elem_id` 精确定位隐藏
- 提交并推送至 GitHub（commit `b9671ca`）

---

## 说明

| 角色 | 姓名 | 负责模块 |
|:---:|------|----------|
| 组长（C） | 张建宇 | 主程序 + YOLO 物体检测 + 集成 |
| 组员 A | 岳思铭 | 图像分类 + 文字识别（OCR） |
| 组员 B | 李汇洋 | 人脸检测 + 启动脚本 + 风格迁移 |
